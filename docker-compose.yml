services:
  monitor:
    build: .
    container_name: force-push-monitor
    restart: unless-stopped
    env_file: .env
    volumes:
      - fpdata:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  fpdata:
